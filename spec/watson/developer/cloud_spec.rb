require "spec_helper"

RSpec.describe Watson::Developer::Cloud do
  it "has a version number" do
    expect(Watson::Developer::Cloud::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
